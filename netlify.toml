[build]
  base = "web"
  command = "npm run build"
  publish = "dist"

# React Router uses client-side routing - send every path to index.html
# so refreshing on e.g. /orders/5 doesn't 404.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
